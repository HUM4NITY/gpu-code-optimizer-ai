import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "GPU Code Optimizer | NVIDIA Nemotron AI",
  description: "AI-powered GPU code analysis and optimization suggestions using NVIDIA Nemotron models. Optimize CUDA, Python, and C++ code for maximum performance.",
  keywords: ["GPU", "CUDA", "optimization", "NVIDIA", "Nemotron", "AI", "code analysis"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
